<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TryOn" Height="600" Width="1100" 
        WindowStartupLocation="CenterScreen" 
        Background="White"
        WindowStyle="None"
        ResizeMode="CanMinimize">

    <Grid>
        <!-- Capa 1: Imagen de fondo -->
        <Image Source="Recursos/fashion_main.jpg" Stretch="UniformToFill" Margin="0,0,0,177"/>

        <!-- Capa 2: Header flotante transparente -->
        <Grid Height="70" VerticalAlignment="Top" Background="#80FFFFFF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock Text="Try" FontSize="24" FontWeight="Bold" Foreground="Black"/>
                <Ellipse Width="24" Height="24" Fill="Black" Margin="5,0"/>
                <TextBlock Text="n" FontSize="24" FontWeight="Bold" Foreground="Black"/>
            </StackPanel>

            <!-- Botones de Sign In y Sign Up -->
            <Button Grid.Column="2" 
                    Content="Sign In" 
                    Width="100"
                    Height="35"
                    Background="#632024"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Margin="0,0,10,0"
                    Click="SignIn_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6F4D38"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#4A1A1D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="3" 
                    Content="Sign Up" 
                    Width="100"
                    Height="35"
                    Background="#6F4D38"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Margin="0,0,20,0"
                    Click="SignUp_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7F5D48"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5F3D28"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Capa 3: Panel blanco con texto y botón -->
        <Border Background="White" 
                Width="1090" 
                Height="180" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Left" 
                Margin="10,420,0,0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="STREET WEAR" 
                           FontSize="48" 
                           FontWeight="ExtraBold" 
                           Foreground="Black" RenderTransformOrigin="0.5,0.5" 
                           >
                    <TextBlock.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform AngleX="-1.483"/>
                            <RotateTransform/>
                            <TranslateTransform X="0.826"/>
                        </TransformGroup>
                    </TextBlock.RenderTransform>
                </TextBlock>
                <Border Background="#632024" 
                        CornerRadius="20" 
                        Width="150" 
                        Height="40" 
                        Margin="0,20,0,0"
                        HorizontalAlignment="Center">
                    <Button Content="Ver Coleccion" 
                            Background="Transparent" 
                            Foreground="White" 
                            BorderThickness="0" 
                            FontWeight="SemiBold"
                            Click="VerColeccion_Click"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Botones de control de ventana -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="0,5,5,0">
            <Button x:Name="MinimizeButton" 
                    Content="—" 
                    Width="30" 
                    Height="30" 
                    Background="Transparent" 
                    Foreground="Black" 
                    BorderThickness="0" 
                    Click="MinimizeButton_Click"/>
            <Button x:Name="CloseButton" 
                    Content="✕" 
                    Width="30" 
                    Height="30" 
                    Background="Transparent" 
                    Foreground="Black" 
                    BorderThickness="0" 
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>